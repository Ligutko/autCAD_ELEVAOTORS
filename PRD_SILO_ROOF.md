# PRD. Дах секторами і кадр зсередини

Дата: 2026-09-23
Зріз: **SILO-ROOF**. Та сама ємність. Норію не будувати.
Перевірка по кадрах і по blend.

Брати логіку стіни з уже прийнятого `blender/silo_msvu_220_seam/`: 14 поясів, фланець, 56 болтів із окремих головок і тіл, напуск, D = 22.000 м, H = 21.422 м, плоске днище. Той blend не перезаписувати. Нова тека:

```
blender/silo_msvu_220_roof/
  build_silo_roof.py
  silo_msvu_220_roof.blend
  measure.json
  render_exterior.png
  render_open.png
  render_inside.png
  build_report.md
```

`blender/silo_msvu_220/`, `blender/silo_msvu_220_sheets/`, `blender/proof/`, `inbox/` не чіпати. `FACTS.json` не створювати.

---

## Дах

Замість одного диска `SILO_ROOF` зробити **8 окремих секторів** `SILO_ROOF_01` … `SILO_ROOF_08`.

- Лежать на Z = 21.422 м. Ухилу немає: сектор плоский. Кута в паспорті немає, конус не малювати.
- Разом вони закривають круг радіусом 11.000 м.
- Між секторами щілина 0.04 м, щоб шви читались.
- У центрі отвір радіусом 0.4 м, без вигаданого вентилятора.
- Усі ці розміри в JSON з `from_passport: false`.

Напис додає рядки:

```
дах — 8 плоских секторів, ухил не з паспорта
немає факту: хвиля гофри, товщина листа
```

Решта напису як у зрізі стику: діаметр, висота, плоске днище, фланець і напуск як показ.

---

## Кадри

1280×720, `BLENDER_EEVEE_NEXT`. Камера загального виду не вище Z = 16 м.

| Файл | Що видно |
|---|---|
| `render_exterior.png` | Увесь силос, вертикальний фланець, шви секторів даху, напис |
| `render_open.png` | Сектор `SILO_ROOF_01` схований. У дірку видно порожнину і плоску підлогу. Інші сектори на місці |
| `render_inside.png` | Камера всередині, приблизно X = 0, Y = -4 м, Z = 2 м, дивиться на підлогу і внутрішню стіну. Видно пояси зсередини і диск підлоги. Конуса немає |

Повна висота стіни 21.422 ± 0.0001 м, низ на 0, зовнішній діаметр стіни 22.000 ± 0.0001 м. Хвилі гофри немає. Висоту 22.096 м не використовувати.

`measure.json`: `roof_sectors: 8`, `roof_slope_from_passport: false`, `from_passport: false`, `corrugation_wave: false`, виміряні діаметр і висота з сітки. Якщо розмір стіни поза допуском, `SILO_FAIL` і код 2. Інакше три кадри і `SILO_PASS`.

Перший рядок відповіді: шлях до `blender/silo_msvu_220_roof/measure.json` і `SILO_PASS` або `SILO_FAIL`.
