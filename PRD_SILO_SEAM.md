# PRD. Стик поясів, який читається як кріплення

Дата: 2026-09-23
Зріз: **SILO-SEAM**. Та сама ємність. Нових вузлів немає.
Перевірка після здачі йде по крупному кадру і по blend, не по переказу агента.

Вихідна модель, яку не перезаписувати: `blender/silo_msvu_220_sheets/`.
Гладка оболонка `blender/silo_msvu_220/` і `blender/proof/` теж не чіпати.
Канон вигляду кріплення: `canon/detail/03_noria_bolted_casing.jpg` і `canon/detail/06_bolts_double_floor.jpg`.

---

## 1. Навіщо

14 поясів уже стоять. Діаметр 22.000 м і висота 21.422 м зійшлися. На крупному кадрі болт вийшов цяткою, а горизонтальний шов — тонкою смугою. Цей зріз робить один вертикальний стик і напуск листа такими, щоб на кадрі було видно метал і кріплення.

Крок пояса, діаметр болта і ширина фланця лишаються показом. У паспорті їх немає. У JSON прапорець `from_passport: false`. Хвилю гофри не додавати.

---

## 2. Що лишається як було

- 14 поясів, сумарно від Z = 0 до Z = 21.422 м, допуск 0.0001 м.
- Зовнішній радіус стіни 11.000 м. Допуск діаметра 22.000 ± 0.0001 м.
- Плоске днище і окрема плоска кришка. Конуса немає. Норії немає.
- Висоту 22.096 м не використовувати. Ярусів не робити 13.
- Напис і далі каже, що крок і болт не з паспорта.

---

## 3. Що змінити

Нова тека, стара модель лишається цілою:

```
blender/silo_msvu_220_seam/
  build_silo_seam.py
  silo_msvu_220_seam.blend
  measure.json
  render_exterior.png
  render_seam.png
  render_side.png
  build_report.md
```

### Вертикальний фланець

Один об’єкт `SILO_FLANGE`. Смуга металу вздовж додатного X, на всю висоту стіни.

- Ширина смуги 0.35 м.
- Виступає назовні від радіуса 11.000 м на 0.12 м.
- Товщина смуги для показу 0.02 м.
- Усі три числа в JSON з `from_passport: false`. Це не заводський фланець, це щоб стик читався.

### Болти на фланці

Два вертикальні ряди, по краях смуги. На кожен пояс 4 болти: два зліва, два справа. Разом 56 болтів, імена `SILO_BOLT_01` … `SILO_BOLT_56`.

Кожен болт складається з двох мешів, не з однієї кульки:

- головка: циліндр діаметром 0.12 м, висота 0.04 м
- тіло під головкою: циліндр діаметром 0.05 м, видно на 0.03 м

Діаметри екранні, щоб на кадрі з двох поясів головка була предметом, а не пікселем. У JSON: `display_bolt_head_diameter_m: 0.12`, `from_passport: false`.

Головки стоять на зовнішній грані фланця, не втоплені в стіну.

### Напуск поясів

Кожен верхній пояс заходить на нижній на 0.08 м. Нижній край верхнього листа і верхній край нижнього читаються як два шари, не як одна лінія. Напуск теж показ, `from_passport: false`. Повна висота стоса від цього не змінюється: низ лишається 0, верх 21.422 м.

Хвилі на листі немає. Сегментів кола на пояс не більше 48.

---

## 4. Кадри

1280×720, `BLENDER_EEVEE_NEXT`.

| Файл | Що в кадрі |
|---|---|
| `render_seam.png` | Тільки два пояси і фланець між ними. У кадрі видно 8 головок. Головка займає помітне місце, її можна відрізнити від тіла болта. Це головний кадр зрізу |
| `render_exterior.png` | Увесь силос від підлоги до кришки, камера не вище Z = 16 м. Фланець читається як вертикальна смуга. Напис видно |
| `render_side.png` | Ортографія. Силует без конуса, висота та сама |

Напис `SILO_LABEL`:

```
МСВУ 220.13.В12
D = 22.000 м
H = 21.422 м
днище плоске
фланець і болти — показ, розмір не з паспорта
напуск поясів — показ, не з паспорта
немає факту: хвиля гофри, товщина листа, ухил даху
```

---

## 5. measure.json

Числа діаметра і висоти зняти з сітки після оновлення сцени.

```json
{
  "pass": true,
  "ring_count": 14,
  "bolt_count": 56,
  "flange": true,
  "wall_z_min_m": 0,
  "wall_z_max_m": 21.422,
  "outer_diameter_m": 22.0,
  "display_flange_width_m": 0.35,
  "display_bolt_head_diameter_m": 0.12,
  "display_lap_m": 0.08,
  "from_passport": false,
  "corrugation_wave": false,
  "rejected_height_m": 22.096
}
```

Якщо діаметр або повна висота поза допуском, друкувати `SILO_FAIL` і виходити з кодом 2. Якщо зійшлося: три кадри, blend, `SILO_PASS`.

`build_report.md` починається шляхом до `measure.json` і одним абзацом: фланець є, болтів 56, напуск 0.08 м, усе це показ, хвилі гофри немає, попередні blend не перезаписані.

---

## 6. Заборони

Не змінювати `inbox/records/`, не створювати `FACTS.json`, не запускати парсер, не відкривати Blender вікном, не слати команди на порт 9876. Не копіювати розміри з кадрів Demetra. Не малювати норію, драбину, люк, дах секторами. Не підписувати 0.12 м як заводський болт.

---

## 7. Коли зріз прийнято

1. `from_passport` дорівнює false, хвилі гофри немає.
2. Висота стоса 21.422 ± 0.0001 м, діаметр стіни 22.000 ± 0.0001 м.
3. Є `SILO_FLANGE` і 56 болтів, у кожного головка окремо від тіла.
4. На `render_seam.png` два пояси, фланець і головки, які не зливаються в крапки.
5. На загальному кадрі є вертикальна смуга стику і напис «не з паспорта».
6. `blender/silo_msvu_220_sheets/silo_msvu_220_sheets.blend` не змінений цим прогоном.
7. Карток як і раніше 192, усі `unverified`. `FACTS.json` немає.

Не прийнято, якщо на крупному кадрі знову ряд цяток без фланця.

Перший рядок відповіді: шлях до `blender/silo_msvu_220_seam/measure.json` і `SILO_PASS` або `SILO_FAIL`.
