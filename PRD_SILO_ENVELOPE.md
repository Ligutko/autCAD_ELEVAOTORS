# PRD. Оболонка силоса МСВУ 220.13.В12

Дата: 2026-09-23
Статус: до виконання новим агентом. Після здачі цей файл перевіряють окремо, по артефактах, без довіри до звіту агента.
Зріз: **SILO-ENVELOPE**. Один силос. Ні норії, ні другого силоса, ні майданчика.

Читати перед роботою: цей файл цілком. Історію старих скриптів AutoCAD не читати як зразок.

---

## 1. Навіщо

Потрібна перша чесна оболонка силоса в Blender, у метрах, у яку пізніше можна завести камеру. Зараз закривається тільки те, що вже зійшлося між нашим кресленням і сторінкою заводу.

Перевірка буде зовнішня. Агент здає файли. Інший прохід міряє їх сам і каже «прийнято» або «не прийнято». Тому кожне число має лежати в JSON, зняте з об’єкта після побудови, а не бути переказане в тексті.

---

## 2. Що вже зроблено і що не переробляти

| Факт | Де лежить | Що з цим робити |
|---|---|---|
| Blender 4.5.2 LTS, команда `blender` | `C:\Program Files\Blender Foundation\Blender 4.5\blender.exe` | Запускати headless. GUI не відкривати |
| Доказ фази 0, куб 3.5×2.0×1.25 м | `blender/proof/` | Не перезаписувати, не відкривати той blend |
| 187 локальних карток + 5 веб-карток, усі `unverified` | `inbox/records/` | Не змінювати статус, не редагувати JSON |
| Звіт дірок і звіт SLICE-2A | `inbox/reports/gaps.md`, `inbox/reports/slice2a_search.md` | Читати можна. Не переписувати |
| `FACTS.json` | файлу немає | Не створювати |

Сокет MCP (`localhost:9876`) цьому зрізу не потрібен. Працювати через `blender --background`.

---

## 3. Які числа дозволені

Тільки ці. Інших розмірів у сітці бути не може.

| Поле | Значення | Звідки | Як використовувати |
|---|---|---|---|
| Діаметр корпусу | 22.000 м | `Plant3D_Equipment_Catalog.json`, тег МСВУ 220.13.В12, поле diameter 22000 мм. Той самий рядок OCR на листах 8, 10, 12: «D=22,0 м» | Зовнішній діаметр стіни |
| Висота | 21.422 м | Каталог, height 21422 мм. OCR: «Н=21,422 м» | Висота стіни від підлоги до низу кришки |
| Днище | плоске | Картка `rec_7f7031e0`, сторінка заводу серії з плоским дном. Статус картки лишається `unverified` | Диск підлоги на Z = 0. Конуса немає |
| Виробник на підписі | Лубнимаш | Картка `rec_e89bebfa`. Статус не змінювати | Лише текст у кадрі, не окрема геометрія |
| Об’єм | 6381 м³ | Каталог і той самий рядок OCR | Тільки рядок у звіті. Геометрію під об’єм не підганяти |

Об’єм циліндра діаметром 22 м і висотою 21.422 м більший за 6381 м³. Це не помилка побудови. 6381 м³ — паспортна місткість, не об’єм оболонки до верху стіни. Висоту не вкорочувати і конус не додавати, щоб «зійтися з кубами».

Рядок з оголошення bizorg `МСВУ 220.13.В12 13 22096 6381 5105` містить висоту 22.096 м. У модель її не брати. 22.096 м не дорівнює 21.422 м. Карточку ярусів «13» теж не втілювати кільцями на стіні: джерело — оголошення 2017 року, не паспорт заводу.

Заборонені в сітці, бо факту немає:

- крок гофри, хвиля на стіні, вертикальні ребра «як у силоса»
- товщина листа, модифікатор Solidify з будь-яким міліметром
- болти, фланці, драбина, люк, вентилятори, канали аерації
- ухил даху. Дах — плоска кришка-заглушка, не конус
- норія, транспортер, другий силос, майданчик, земля з рельєфом

---

## 4. Геометрія

Одиниці сцени: Metric, довжина в метрах, `scale_length = 1.0`. Вісь силоса: X = 0, Y = 0. Підлога на Z = 0.

Три окремі об’єкти. Імена точно такі:

| Об’єкт | Геометрія | Вершин |
|---|---|---|
| `SILO_WALL` | Циліндр без верхньої і без нижньої кришки. Радіус 11.000 м. Висота 21.422 м. Низ на Z = 0, верх на Z = 21.422 | 64 сегменти кола. Два кільця вершин, тобто 128 вершин. Не більше |
| `SILO_FLOOR` | Заповнений диск, радіус 11.000 м, лежить на Z = 0 | 64 сегменти по колу |
| `SILO_ROOF` | Окремий диск, радіус 11.000 м, лежить на Z = 21.422. Це заглушка, не дах із ухилом | 64 сегменти |

`SILO_ROOF` не батько стіни і не з’єднаний з нею в один меш. Його можна сховати, не ховаючи стіну.

Порожнина всередині порожня: жодного зерна, перекриття, конуса, колони.

Текст у сцені, об’єкт `SILO_LABEL`, читається на зовнішньому кадрі:

```
МСВУ 220.13.В12
Лубнимаш
D = 22.000 м
H = 21.422 м
днище плоске
немає факту: гофра, товщина листа, болти, ухил даху
```

Підлога під моделлю, об’єкт `GROUND`, розмір не менше 60×60 м, на Z = 0, не входить у вимірювання силоса. Матеріал сірий, стіна іншого кольору, кришка третього, щоб на кадрі було видно шов між стіною і кришкою.

Жодного іншог об’єкта з мешем, крім цих чотирьох і напису. Світло і камери не рахуються мешами.

---

## 5. Кадри

Три PNG, 1280×720, рушій `BLENDER_EEVEE_NEXT`.

| Файл | Що має бути видно |
|---|---|
| `render_exterior.png` | Увесь силос у три чверті, від підлоги до кришки, напис читається |
| `render_open.png` | Той самий ракурс, об’єкт `SILO_ROOF` схований. Видно порожнину і плоску підлогу. Конуса в кадрі немає |
| `render_side.png` | Ортографічний вид збоку. Силос від Z = 0 до Z = 21.422, без перспективи, щоб висота не «з’їхала» від ракурсу |

Камера не всередині стіни. На `render_open.png` підлога має читатися як диск, не як лійка.

---

## 6. Файли здачі

Усе нове лише тут:

```
blender/silo_msvu_220/
  build_silo_envelope.py
  silo_msvu_220.blend
  measure.json
  render_exterior.png
  render_open.png
  render_side.png
  build_report.md
```

Команда, якою агент сам перевіряє збірку перед здачею:

```
blender --background --factory-startup --python "d:\autocad project\blender\silo_msvu_220\build_silo_envelope.py"
```

Скрипт:

1. Будує сцену з нуля (`--factory-startup` уже дає порожню фабрику; додатково не підвантажувати `blender/proof/phase0_cube.blend`).
2. Ставить три частини і напис.
3. Після `view_layer.update()` читає габарити з об’єктів, не з констант, які сам щойно записав у змінну.
4. Пише `measure.json`.
5. Якщо хоч один розмір поза допуском 0.0001 м, друкує `SILO_FAIL` і виходить з кодом 2. Blend успішної здачі в цьому випадку не залишати: або не зберігати, або зберегти поруч як `silo_msvu_220.FAILED.blend` і написати це в звіт.
6. Якщо розміри в допуску, рендерить три кадри, ховає кришку тільки на час другого кадру і повертає її видимою перед збереженням blend.
7. Зберігає blend. Друкує `SILO_PASS`.

`measure.json` має бути таким за формою. Значення `measured_*` знімаються з Blender:

```json
{
  "pass": true,
  "unit_system": "METRIC",
  "length_unit": "METERS",
  "scale_length": 1.0,
  "tolerance_m": 0.0001,
  "wall": {
    "name": "SILO_WALL",
    "vertex_count": 128,
    "target_diameter_m": 22.0,
    "target_height_m": 21.422,
    "measured_diameter_m": 0,
    "measured_height_m": 0,
    "z_min_m": 0,
    "z_max_m": 0
  },
  "floor": {
    "name": "SILO_FLOOR",
    "measured_diameter_m": 0,
    "z_m": 0
  },
  "roof": {
    "name": "SILO_ROOF",
    "measured_diameter_m": 0,
    "z_m": 0,
    "is_separate_object": true
  },
  "catalog_volume_m3": 6381,
  "shell_cylinder_volume_m3": 0,
  "volume_note": "6381 is rated capacity, not the volume of this shell",
  "not_modeled": [
    "corrugation_pitch",
    "sheet_thickness",
    "bolt_pattern",
    "roof_slope",
    "cone",
    "noria"
  ],
  "rejected_numbers": [
    {"value_m": 22.096, "why": "bizorg row height, disagrees with drawing 21.422 m"}
  ]
}
```

`shell_cylinder_volume_m3` рахувати як π × 11² × 21.422 і записати округлення до 0.01. Це пояснення розбіжності, не ціль підгонки.

`build_report.md` починається шляхом `blender/silo_msvu_220/measure.json` і далі коротко: виміряні D і H, число вершин стіни, що кришка окрема, що конуса немає, що 22.096 м відкинуто, що статуси карток не змінювались.

---

## 7. Заборони

1. Не ставити статус `cited` або `rejected` жодній картці в `inbox/records/`.
2. Не створювати `FACTS.json` і `SITE.json`.
3. Не змінювати `inbox/reports/gaps.json`, `gaps.md`, `parse_log.json`, `slice2a_search.md`.
4. Не запускати `inbox/parse_local.py`.
5. Не писати в `blender/proof/`.
6. Не читати DXF і не переводити міліметри листа в метри об’єкта.
7. Не відкривати Blender вікном і не слати команди на порт 9876.
8. Не імпортувати моделі з інтернету, з `D:\ELEVATOR_REFERENCES\` і з каталогу 2021 року Лубнимаша. Той PDF лишається джерелом пошуку, не сіткою.
9. Не додавати гофру «для вигляду», навіть дрібну і навіть через Geometry Nodes.
10. Не ставити товщину стіни «типову для галузі» (1 мм, 1.2 мм, 2 мм, 3 мм). Товщини в фактах немає, тож стінка без товщини.
11. Не згладжувати розбіжність 6381 м³ проти об’єму циліндра.
12. Не будувати норію, навіть з кроку 180 мм і шахти 376×256 мм. Ці числа з оголошення, не з паспорта, і в цей зріз не входять.

---

## 8. Коли агент зупиняється

Після `SILO_PASS`, трьох PNG, blend і `build_report.md`. Наступний силос, норія, обліт камери всередину, затвердження карток і пошук гофри в інтернеті — не цей зріз.

Перший рядок відповіді людині: шлях до `blender/silo_msvu_220/measure.json` і рядок `SILO_PASS` або `SILO_FAIL`.

---

## 9. Як це перевірятимуть

Перевіряльник не вірить `build_report.md`, доки не збіглися файли.

Прийнято, коли одночасно:

1. `measure.json` існує, `pass` дорівнює true.
2. Виміряний діаметр стіни, підлоги і кришки кожен в межах 22.000 ± 0.0001 м.
3. Висота стіни в межах 21.422 ± 0.0001 м. `z_min_m` стіни дорівнює 0, `z_max_m` дорівнює висоті.
4. `vertex_count` стіни дорівнює 128. Більше означає, що в сітку додано гофру, ребра або зайву товщину.
5. У blend три окремі об’єкти з іменами `SILO_WALL`, `SILO_FLOOR`, `SILO_ROOF`.
6. На `render_open.png` видно порожню трубу і плоске дно. Конуса немає.
7. На `render_exterior.png` читається напис із рядком «немає факту».
8. У `measure.json` є відкинуте число 22.096 м.
9. `blender/proof/phase0_cube.png` і `phase0_report.json` не змінені цим прогоном (час запису лишається 2026-09-23 8:20).
10. Кількість карток у `inbox/records/` лишається 192, усі зі статусом `unverified`. `FACTS.json` у корені проєкту немає.

Не прийнято, навіть якщо картинка гарна, коли стіна має хвилю, товщину, конус, висоту 22.096 м або другий силос.
