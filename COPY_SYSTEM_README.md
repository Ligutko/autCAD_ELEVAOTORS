# СИСТЕМА КОПІЮВАННЯ СИЛОСІВ (COPY SYSTEM)

## ЩО ЦЕ ТАКЕ?

Замість **малювання з нуля**, ця система **КОПІЮЄ** існуючі об'єкти з DXF!

## ЯК ЦЕ ПРАЦЮЄ?

### КРОК 1: Вибір шаблону
```python
template_silo = silos[5]  # Силос #6 як шаблон
```

### КРОК 2: Пошук всіх пов'язаних об'єктів
Знаходить ВСЕ в радіусі 60мм від центру силосу:
- **Синіх полілініï** (сам силос)
- **Червоних полілініï** (норії/конвеєри)
- **Заливок** (hatches)
- **Інших елементів** (обладнання)

### КРОК 3: Копіювання
```python
copied = original.Copy()          # Копіюємо COM-об'єкт
copied.Move(cp(0,0,0), cp(dx,dy,0))  # Переміщуємо
```

### КРОК 4: Розрахунок позиції
```python
spacing = silo6_x - silo5_x  # Відстань між 5-м та 6-м
offset_x = spacing            # Така ж відстань для 7-го
```

---

## РЕЗУЛЬТАТ ТЕСТУ

### Що скопіювалось для 7-го силосу:

```
Shablon: Sylos #6 (x=317.82, y=63.54)
Nova pozytsiia: x=365.57, y=63.54

Skopiiovano obiektiv: 223
  - Synikh (sylos): 39
  - Chervonykh (noriyi/konveyory): 25
  - Zalivok (hatches): 159
```

---

## ПЕРЕВАГИ ЦІЄЇ СИСТЕМИ

### ДО (генерація з нуля):
❌ Малює тільки прямокутники
❌ Немає норій, конвеєрів
❌ Немає заливок, деталей
❌ Треба знати як малювати кожен елемент

### ПІСЛЯ (копіювання):
✅ **Копіює ПОВНИЙ силос** зі всіма деталями
✅ **Норії та конвеєри** копіюються автоматично
✅ **Заливки** (hatches) зберігаються
✅ **Не треба знати** як малювати - просто копіюємо!

---

## ВИКОРИСТАННЯ

### Додати 7-й силос:
```bash
python copy_silo_system.py
```

### Додати N силосів:
```python
system = SiloCopySystem()
system.connect_autocad()
system.load_silos()

# Додати 7-й
system.copy_silo(template=6, position=7, offset_x=47.75)

# Додати 8-й
system.copy_silo(template=6, position=8, offset_x=95.5)

# Додати 9-й
system.copy_silo(template=6, position=9, offset_x=143.25)
```

---

## НАСТУПНІ КРОКИ

1. **Автоматичне з'єднання конвеєрів**
   - Знайти "кінцеві точки" скопійованих конвеєрів
   - Додати лінії для з'єднання з існуючою системою

2. **Копіювання різних конфігурацій**
   - Копіювати групу силосів (2-3-4)
   - Дзеркальне відображення

3. **Параметричне масштабування**
   - Змінювати розмір при копіюванні
   - D20 → D22 автоматично

---

## ФАЙЛИ

| Файл | Призначення |
|------|-------------|
| `copy_silo_system.py` | Головна система копіювання |
| `extracted_silos_AUTO.json` | Координати 6 силосів |
| `extracted_red_objects.json` | Норії та конвеєри |

---

**ЦЕ ПРАВИЛЬНИЙ ПІДХІД - КОПІЮВАННЯ ЗАМІСТЬ ГЕНЕРАЦІЇ!** 🎯
