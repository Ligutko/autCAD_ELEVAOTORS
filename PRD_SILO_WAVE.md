# PRD. Хвиля 64 мм і дах 30°

Дата: 2026-09-23
Зріз: **SILO-WAVE**. Та сама ємність. Норію не будувати.
Числа: `inbox/reports/lubnymash_sheet_roof.md`.

Не перезаписувати `blender/silo_msvu_220_tiers/`. Нова тека `blender/silo_msvu_220_wave/`.

Стіна: 13 поясів, корисна висота пояса 1.152 м, верх стіни 14.976 м, зовнішній діаметр 22.000 м, плоске днище на Z = 0. На кожному поясі хвиля з кроком 64 мм. 1.152 / 0.064 = 18 хвиль на пояс. Висоту хвилі завод не дав. Для картинки взяти 12 мм і підписати: крок 64 мм з каталогу Лубнимаша, висота хвилі не з каталогу.

Товщину листа в геометрію не закладати окремим числом яруса. Якщо потрібна товщина тіла, 2 мм, і в написі: «товщина стіни 1–3 мм за ярусом, 2 мм лише всередині діапазону».

Дах: конус 30° до горизонту від радіуса 11 м. Підйом близько 6.351 м. Верх конуса близько Z = 21.327 м. Мітка патрубка лишається на Z = 21.422 м. Між вершиною даху і міткою короткий зазор, не друга порожня шахта на 6 м. Сектори даху можна лишити вісьмома, але вони лежать на скаті, не плоским диском. Карниз 0.9 м не додавати.

Фланець і болти як у SILO-TIERS, тільки на висоту стіни. `FACTS.json` не створювати.

Кадри: зовні видно хвилю, скат і мітку патрубка; зсередини плоска підлога і хвиля на стіні; один сектор даху можна сховати.

`measure.json`: `wave_pitch_m: 0.064`, `waves_per_ring: 18`, `roof_slope_deg: 30`, `wall_z_max_m: 14.976`, `roof_peak_z_m` з сітки, `from_catalog: true` для кроку і кута, `wave_height_from_catalog: false`. Якщо дах знову плоский на 14.976 м, це `SILO_FAIL`.

Перший рядок відповіді: шлях до `blender/silo_msvu_220_wave/measure.json` і `SILO_PASS` або `SILO_FAIL`.
