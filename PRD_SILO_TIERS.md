# PRD. Стіна з 13 поясів по 1,152 м

Дата: 2026-09-23
Зріз: **SILO-TIERS**. Дах секторами лишається. Норію не будувати.

Прийнятий дах не перезаписувати: `blender/silo_msvu_220_roof/`.
Стик, гладку оболонку і `blender/proof/` не чіпати. `FACTS.json` не створювати. Картки в `inbox/records/` не змінювати.

Звідки числа: `inbox/reports/lubnymash_220_tiers.md`. Сторінка заводу дає для МСВУ.220 висоту 15662–31790 мм і 8–22 яруси. Крок 1152 мм виходить діленням цього діапазону. 13 ярусів дають рівно 21422 мм від нуля до завантажувального патрубка. На сторінці немає окремого речення «пояс = 1152 мм». У моделі це підписати як розрахунок з таблиці, не як рядок креслення.

---

## Розкладка

| Частина | Розмір |
|---|---|
| Поясів | 13 |
| Висота одного пояса | 1.152 м |
| Верх стіни | 14.976 м |
| Від верху стіни до патрубка | 6.446 м порожні |
| Повна висота до патрубка | 21.422 м |
| Зовнішній діаметр стіни | 22.000 м |
| Днище | плоске, Z = 0 |

Порожні 6,446 м не заповнювати конусом і не заповнювати додатковими поясами. Градуса даху досі немає. На рівні Z = 21.422 поставити лише тонке кільце-мітку `SILO_SPOUT_MARK` і напис, що між дахом і патрубком конструкції ще немає.

Фланець і болти лишаються показом, як у стику: один фланець на висоту стіни (до 14.976 м, не до 21.422), по 4 болти на пояс, головка окремо від тіла. Діаметр головки 0.12 м, `from_passport: false`. Напуск поясів 0.08 м, теж показ.

Дах: 8 плоских секторів на верху стіни, Z = 14.976 м, не на 21.422 м. Щілина 0.04 м, отвір у центрі радіусом 0.4 м. Карниз 0.9 м не повторювати. Ухилу немає.

Хвилі гофри немає. Висоту 22.096 м не використовувати. Поясів не робити 14.

---

## Файли

```
blender/silo_msvu_220_tiers/
  build_silo_tiers.py
  silo_msvu_220_tiers.blend
  measure.json
  render_exterior.png
  render_open.png
  render_inside.png
  build_report.md
```

Напис:

```
МСВУ 220.13.В12
D = 22.000 м
стіна 13 x 1.152 м = 14.976 м
до патрубка 21.422 м, між дахом і патрубком порожньо
13 і 1.152 м — з діапазону таблиці Лубнимаша, не з окремого рядка
немає факту: хвиля гофри, товщина листа, ухил даху
```

Кадри 1280×720. Камера загального виду має показати і верх стіни, і мітку патрубка вище за неї, щоб було видно порожній проміжок. `render_open.png`: один сектор схований, видно плоску підлогу. `render_inside.png`: камера біля підлоги, пояси зсередини, без конуса.

`measure.json` знімає висоту стіни і діаметр із сітки. Допуск 0.001 м. Очікувані поля: `ring_count: 13`, `tier_height_m: 1.152`, `wall_z_max_m: 14.976`, `spout_z_m: 21.422`, `roof_z_m: 14.976`, `from_passport: false`, `corrugation_wave: false`. Якщо стіна знову доходить до 21.422 м, це `SILO_FAIL`.

Перший рядок відповіді: шлях до `blender/silo_msvu_220_tiers/measure.json` і `SILO_PASS` або `SILO_FAIL`.
