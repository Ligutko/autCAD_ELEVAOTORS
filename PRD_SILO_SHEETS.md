# PRD. Силос з поясів листів

Дата: 2026-09-23
Статус: до виконання новим агентом. Після здачі перевірка йде по кадру і по `measure.json`, не по переказу.
Зріз: **SILO-SHEETS**. Одна ємність МСВУ 220.13.В12. Норію, галерею і другий силос не будувати.

Канон, який цей зріз має почати закривати: `CANON_DETAIL.md`.
Оболонка, яку не псувати: `blender/silo_msvu_220/`.

---

## 1. Навіщо

Гладкий циліндр уже перевірений. Він лишається рівнем 0. Цей зріз робить стіну, яку з камери можна прочитати як стос листів зі стиком, а не як трубу.

Розміри корпусу не змінюються: зовнішній діаметр 22.000 м, висота стіни 21.422 м, підлога на Z = 0, днище плоске.

Кроку гофри і діаметра болта в паспорті немає. Їх не вигадувати як факт. Пояси і один ряд кріплення показуються як тип конструкції. У кадрі і в JSON прямо написано, що крок пояса і розмір болта не з паспорта.

---

## 2. Що не чіпати

- `blender/silo_msvu_220/` і `blender/proof/` не перезаписувати.
- Картки в `inbox/records/` не змінювати. Статус лишається `unverified`.
- `FACTS.json` не створювати.
- Висоту 22.096 м не використовувати.
- Число ярусів 13 з оголошення bizorg не використовувати як кількість поясів.
- Хвилю гофри (синусоїду на листі) не моделювати. Числа хвилі немає.
- Норію, транспортер, драбину, дах із секторів, конус не додавати.

---

## 3. Геометрія стіни

Одиниці: Metric, метри, `scale_length = 1.0`. Вісь X = 0, Y = 0.

Стіна більше не один циліндр на 128 вершин. Вона складається з **14 окремих поясів**. Висота кожного:

`21.422 / 14 = 1.530142857 м`

Це ділення нашої вже перевіреної висоти на рівне число поясів, щоб шов читався. Це не крок заводу. У звіті поле називається `display_ring_height_m`, поруч прапорець `from_passport: false`.

Кожен пояс — окремий об’єкт `SILO_RING_01` … `SILO_RING_14`.

- Відкритий циліндр, без верхньої і нижньої кришки.
- Зовнішній радіус 11.000 м. Допуск діаметра 22.000 ± 0.0001 м.
- Низ першого пояса на Z = 0. Верх чотирнадцятого на Z = 21.422 ± 0.0001 м.
- Між поясами видимий шов: верхній край пояса має обід, який виступає назовні на 0.04 м і заввишки 0.03 м. Це мітка стику, не товщина листа. У JSON ці два числа теж з `from_passport: false`.
- На кожному поясі рівно 32 сегменти кола. Не більше. Хвилі гофри немає, тому вершин на пояс мало: два кільця стіни плюс обід.

Вертикальний стик один, на куті 0° (додатна вісь X). На кожному поясі в цій точці один болт: циліндр головки діаметром 0.08 м і заввишки 0.04 м, об’єкт `SILO_BOLT_01` … `SILO_BOLT_14`. Діаметр 0.08 м є екранним, щоб кріплення читалося здалеку. У JSON: `display_bolt_diameter_m: 0.08`, `from_passport: false`. Болти не ховати в стіну: центр головки зовні радіуса 11.000 м.

`SILO_FLOOR` і `SILO_ROOF` лишаються як у рівні 0: окремі плоскі диски радіусом 11.000 м, підлога на Z = 0, кришка на Z = 21.422. Кришка не входить у меш стіни.

Інших мешів, крім 14 поясів, 14 болтів, підлоги, кришки, землі і напису, немає.

---

## 4. Напис у кадрі

Об’єкт `SILO_LABEL`, читається на загальному кадрі:

```
МСВУ 220.13.В12
D = 22.000 м
H = 21.422 м
днище плоске
14 поясів — показ, крок не з паспорта
болт — показ, діаметр не з паспорта
немає факту: хвиля гофри, товщина листа, ухил даху
```

---

## 5. Камери і кадри

Три PNG, 1280×720, `BLENDER_EEVEE_NEXT`.

| Файл | Камера | Що має бути видно |
|---|---|---|
| `render_exterior.png` | перспектива, око приблизно на половині висоти стіни, не вище Z = 16 м. Відстань така, щоб у кадр увійшов увесь силос від підлоги до кришки | Пояси читаються як стос, не як одна труба. Напис видно |
| `render_seam.png` | ближче до вертикального стику, у кадрі 4–6 поясів і їхні болти | Шов і головки болтів, не весь силос |
| `render_side.png` | ортографія збоку, силует від Z = 0 до Z = 21.422 | Прямокутник тієї самої висоти, що й раніше. Конуса немає |

Камеру з висоти Z = 50 м не ставити. Через неї стіна на минулому кадрі зникла.

---

## 6. Файли здачі

```
blender/silo_msvu_220_sheets/
  build_silo_sheets.py
  silo_msvu_220_sheets.blend
  measure.json
  render_exterior.png
  render_seam.png
  render_side.png
  build_report.md
```

Команда перевірки самим агентом:

```
blender --background --factory-startup --python "d:\autocad project\blender\silo_msvu_220_sheets\build_silo_sheets.py"
```

Скрипт після побудови сам міряє об’єкти і пише `measure.json`. Якщо діаметр або повна висота поза допуском 0.0001 м, друкує `SILO_FAIL` і виходить з кодом 2. Якщо зійшлося, рендерить три кадри, зберігає blend, друкує `SILO_PASS`.

Форма `measure.json`:

```json
{
  "pass": true,
  "ring_count": 14,
  "bolt_count": 14,
  "wall_z_min_m": 0,
  "wall_z_max_m": 21.422,
  "outer_diameter_m": 22.0,
  "display_ring_height_m": 1.530143,
  "display_bolt_diameter_m": 0.08,
  "from_passport": false,
  "corrugation_wave": false,
  "rejected_height_m": 22.096,
  "not_modeled": ["corrugation_wave", "sheet_thickness", "roof_slope", "cone", "noria"]
}
```

`outer_diameter_m` і `wall_z_max_m` знімаються з сітки після `view_layer.update()`, не з константи, яку скрипт щойно задав.

`build_report.md` починається шляхом до `measure.json`. Далі один абзац: 14 поясів, крок не з паспорта, болти екранні, хвилі гофри немає, рівень 0 не перезаписаний.

---

## 7. Коли зріз прийнято

1. `measure.json` має `pass: true`, `from_passport: false`, `corrugation_wave: false`.
2. Повна висота стіни 21.422 ± 0.0001 м, низ на 0. Зовнішній діаметр 22.000 ± 0.0001 м.
3. У blend рівно 14 об’єктів `SILO_RING_01`…`14` і 14 об’єктів `SILO_BOLT_01`…`14`.
4. На `render_exterior.png` поясів більше, ніж один, і видна вся висота.
5. На `render_seam.png` видно ряд болтів на вертикальному стику.
6. На `render_side.png` силует без конуса.
7. Напис містить рядок «не з паспорта».
8. `blender/silo_msvu_220/silo_msvu_220.blend` і `blender/proof/` не змінені цим прогоном.
9. Карток у `inbox/records/` як і раніше 192, усі `unverified`. `FACTS.json` немає.

Не прийнято, якщо стіна знову одна труба, якщо на ній синусоїда «гофри», якщо поясів 13 «як в оголошенні», або якщо 0.08 м підписано як заводський болт.

Перший рядок відповіді людині: шлях до `blender/silo_msvu_220_sheets/measure.json` і `SILO_PASS` або `SILO_FAIL`.
