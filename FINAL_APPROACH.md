# ФІНАЛЬНИЙ ПІДХІД - ПРАВИЛЬНЕ МАСШТАБУВАННЯ

## ПРОБЛЕМА ПОПЕРЕДНІХ СПРОБ

❌ **Спроба 1**: Масштабували силоси навколо їх центрів → все накладалось
❌ **Спроба 2**: Масштабували + переміщували групи об'єктів → конвеєри розірвались
❌ **Спроба 3**: Масштабували ВСЕ в радіусі 150мм → конвеєри та норії здулись/зникли

## ПРАВИЛЬНЕ РІШЕННЯ

### КРОК 1: Визначити що масштабувати
```
МАСШТАБУВАТИ:
- Сині полілінії (C00-00-FF) - самі силоси
- Чорні hatches (C00-00-00) - заливки всередині силосів

НЕ МАСШТАБУВАТИ:
- Червоні полілінії (CFF-00-00) - конвеєри/норії
- Коричневі полілінії (C00-95-25) - інше обладнання
```

### КРОК 2: Алгоритм
1. Для кожного силосу:
   - Знайти **ТІЛЬКИ СИНІ** об'єкти (layer C00-00-FF)
   - Знайти **ТІЛЬКИ ЧОРНІ HATCHES** всередині bounds силосу
   - Масштабувати їх навколо центру силосу
   - Перемістити на нову позицію

2. Видалити **ВСІ ЧЕРВОНІ** об'єкти (старі з'єднання)

3. Намалювати **НОВІ ЧЕРВОНІ** з'єднання між силосами

### КРОК 3: Переміщення
- Перерахувати позиції щоб силоси не накладались
- Переміст и тільки сині+чорні об'єкти
- Червоні перемалювати з нуля

## КОД

```python
def smart_resize_final(silo_id, scale, new_position):
    # 1. Znayty TILKY SYNI obiekty
    blue_objects = find_objects(
        layer='C00-00-FF',
        near_silo=silo_id,
        radius=60
    )

    # 2. Znayty TILKY CHORNI HATCHES vseredyni
    black_hatches = find_hatches_inside(
        silo_bounds=silo.bounds,
        layer='C00-00-00'
    )

    # 3. Masshtabuvaty
    for obj in blue_objects + black_hatches:
        obj.ScaleEntity(silo.center, scale)

    # 4. Peremistyty
    dx = new_position.x - silo.center.x
    for obj in blue_objects + black_hatches:
        obj.Move((0,0,0), (dx, 0, 0))

# 5. Vydalyty VSI CHERVONI
delete_all_red_objects()

# 6. Namalyuvaty NOVI zvyazky
redraw_connections_between_silos()
```

## РЕЗУЛЬТАТ
✅ Силоси правильних розмірів
✅ Силоси не накладаються
✅ Заливки на місцях
✅ Конвеєри та норії НОВІ, правильно з'єднані
