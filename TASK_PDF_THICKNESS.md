# Задача. Товщина листа і крок гофри на наших 12 сторінках

Дата: 2026-09-23
Це паралельний зріз. Blender не відкривати. Жоден файл у `blender/` не змінювати.
Картки в `inbox/records/` не редагувати і не переводити в `cited`. `FACTS.json` не створювати. `parse_local.py` заново не ганяти.

Вхід уже лежить на диску:

- `inbox/raw/ocr/page_01.txt` … `page_12.txt` і відповідні TSV
- `inbox/raw/pdf_pages/page_01.png` … `page_12.png`
- `inbox/reports/gaps.md`

Шукати тільки три речі для МСВУ 220.13.В12:

1. Товщина листа стіни, мм.
2. Крок або висота хвилі гофри, мм.
3. Кут або ухил даху.

Число без прив’язки до силоса не записувати як знахідку. «11 кВт» і довжини транспортерів не чіпати.

## Як шукати

Спочатку текст OCR. Маркери: `товщ`, `лист`, `мм`, `гофр`, `хвил`, `крок`, `дах`, `ухил`, `°`, символ дельти. Якщо рядок підозрілий, відкрити PNG тієї сторінки і перевірити очима, чи OCR не зібрав чужі цифри.

Звіт:

```
inbox/reports/pdf_thickness.md
inbox/reports/pdf_thickness.json
```

На кожне з трьох полів або блок `found` з цитатою, номером сторінки і шляхом до PNG, або `not_found` зі списком сторінок, які переглянуті. Знайдене число лишається непідтвердженим: у звіт не писати, що це вже факт для моделі.

Якщо знахідки немає, так і написати. Не підставляти 76 мм, 114 мм чи «типову» товщину 1.2 мм.

Перший рядок відповіді: шлях до `inbox/reports/pdf_thickness.md` і скільки з трьох полів знайдено.
