# 3D-напрям. Стан і план

Дата: 2026-09-24. Точка входу для людини й наступного агента. Деталі — у файлах, на які тут посилання. Бриф для маркетингу й контенту: `ELEVATOR_3D_BRIEF.md`.

## 1. Ідея

Кінцевий результат: один реальний зерновий комплекс у масштабі 1:1, у який заходить камера. Видно справжні вузли (пояси, болти, норія в розрізі). На тій самій моделі показано дефект, узятий з документа. Модель має витримати погляд людини, яка знає 3D, і людини, яка тримає зерно.

Агенти не «малюють елеватор з голови», а працюють у системі з договорами між кроками (`по3де/обота по 3-д світам.txt`, `PRD_3D_PIPELINE.md`):

```
джерело (наш PDF, паспорт, норма) -> inbox/raw
  -> inbox/records   картка = одне твердження з цитатою, статус unverified
  -> людина: cited / rejected (SLICE-3)
  -> FACTS.json      лише cited
  -> SITE.json       розстановка вузлів
  -> Blender         лише деталі з кіта
  -> QA              міряє сцену, PASS / FAIL
  -> режисер         камера, розріз, ролик
```

Три шари не змішуються. **Кіт** — іменовані деталі в метрах (`blender/kit/`). **Світ** — екземпляри кіта за `SITE.json`. **Розбір** — дефект і текст з `FACTS.json`.

Візуальна планка: `CANON_DETAIL.md` (рівень Inventor: листи, фланці, ряди болтів, розріз норії).

## 2. Об'єкт

`Технологія 06.06.24.pdf`, 12 аркушів, текст кривими. Шість силосів МСВУ 220.13.В12 (D 22 м, 6381 м³) у два ряди: 1–2 вгорі, 3–6 внизу. Між силосами 4 і 5 стоїть вежа норії H6, біля силоса 2 — H5. Над силосами проходить естакада з транспортерами T12/T15, під ними тунелі T13/T16. Приймальна частина з H1–H4 і сепаратором. Норії по 100 т/год.

## 3. Що зроблено

| Етап | Стан | Доказ |
|---|---|---|
| SLICE-1: картки з PDF (OCR) і каталогу | здано | `inbox/parse_local.py`, `inbox/reports/gaps.md` |
| SLICE-2A і дослідження дірок | здано | `inbox/reports/slice2a_search.md`, `noria_dims.md`, `pdf_thickness.md`, `lubnymash_*.md`, `gaps_wider_sources.md` |
| SLICE-3: затвердження людиною, `FACTS.json` | **не почато** | усі картки `unverified` |
| Кіт: силос, 6 кроків (оболонка → пояси → стик → дах → яруси → хвиля) | здано, SILO_PASS | `blender/silo_msvu_220*/measure.json` |
| Кіт: норія поруч із силосом | здано, NORIA_PASS, рівень показу | `blender/noria_01/` |
| **SITE-1: план майданчика з вектора PDF** | **цей захід** | `inbox/reports/site_plan_p2.md` |
| **SLICE-5: збирач зі `SITE.json`** | **цей захід, SITE_PASS** | `blender/site/out/tech_2024_06_06/` |
| **SLICE-6: ворота якості** | **цей захід, QA_TESTS_PASS** | `blender/site/run_qa_tests.py` |
| SLICE-7: нутро силоса, режисер | не почато | — |
| SLICE-8: перший ролик з дефектом | не почато | кандидати в `inbox/reports/cases_three.md` |

## 4. Що додав захід 2026-09-24

**План з вектора.** `inbox/extract_plan_p2.py` читає лінії аркуша 2 через PyMuPDF, без растру і без DXF. Шість кіл силосів підганяються найменшими квадратами. Масштаб узято з R = 11 м: 1 pt = 79.92 мм. Масштаб перевірено на п'яти підписаних розмірах, похибка 0.00–0.01 %:

| Проміжок | Підпис | З вектора |
|---|---|---|
| осі 3–4, 5–6, 1–2 | 24000 | 23999–24000 |
| осі 4–5 через вежу | 29000 | 29001 |
| між рядами | 25500 | 25500 |

Нові картки (усі `unverified`, із `source.extractor`): масштаб, кількість силосів на плані, 5 розмірів між осями, дві відмітки (`-5,100` приямок H6, `+29,000` верх вежі H6).

**Кіт як модулі.** `blender/kit/silo_msvu_220.py` і `noria_n100.py` — геометрія прийнятих зрізів SILO-WAVE і NORIA-1 без зміни чисел. Кожне число має запис «звідки» в `SPEC["provenance"]`. Шляхи й шрифти не прив'язані до `d:\autocad project`.

**Збирач.** `blender/assemble_site.py SITE.json`:
- ставить лише деталі з `kit.REGISTRY`; невідомий тип дає FAIL з назвою деталі;
- не дає світу змінити розмір деталі (`params` заборонено);
- після збірки міряє саму сцену (вершини екземплярів через depsgraph), а не числа з JSON;
- кадри і blend з'являються лише при PASS.

Виміряно на реальному майданчику: D = 22.000 м, стіна 14.976 м, вершина даху 21.327 м, ухил 30°, 13 поясів, 52 болти. Це ті самі числа, що в прийнятому `silo_msvu_220_wave/measure.json`, тож кіт перенесено без втрат.

**Ворота якості.** `python blender/site/run_qa_tests.py`: 2 чисті майданчики проходять, 6 зламаних падають, кожен зі своєї причини (під землею, перетин силосів, невідомий тип, спроба змінити діаметр, повтор id, норія в силосі).

**Баг.** Повторний запуск `parse_local.py` видаляв усі `unverified` картки, яких сам не створив, зокрема 12 веб-карток SLICE-2A. Тепер він видаляє лише власні.

Кадри: `blender/site/out/tech_2024_06_06/render_overview.png`, `render_ground.png`, `render_plan.png` (вид згори, звіряти з аркушем 2).

## 5. Відкриті питання (не вигадувати, вирішує людина або документ)

1. На аркушах 4, 5, 7 верх фундаменту силосів має відмітку +0,600. OCR її не прочитав, тому силоси стоять на ±0,000.
2. H6: дно приямку −5,100 плюс труба 33 м дають верх голови близько +28,2. На фасаді привід стоїть над каркасом +29,000. Можливо, 33 м — це H5, а в H6 інша труба: на аркуші 10 є Нтруб = 32500.
3. H5 не зібрана: відмітку −4,700 OCR прочитав як «A700».
4. Норія в кіті — одна труба 376×256 мм з оголошення. Реальна УН має дві труби і вежу 4400. Потрібен паспорт (`inbox/reports/noria_dims.md`: 0 з 5 полів знайдено).
5. Конфлікт потужності У13-ТЦС-320: 11 кВт проти 15 кВт.

## 6. Наступні кроки, за пріоритетом

1. **SLICE-3, затвердження.** Людина проходить картки, від яких залежить `SITE_tech_2024_06_06.json` (їхні id є в полі `cards` кожного вузла), і ставить `cited` або `rejected`. Далі скрипт `inbox/build_facts.py` збирає `FACTS.json`, а збирач отримує строгий режим: читає лише FACTS. Агент статус `cited` не ставить.
2. **Кіт майданчика:** вежа норії (шахта 4400, верх +29,000/+30,000), естакада (+23,200…+25,600), тунель (−1,900, L 48,7 м). Розміри брати з розрізів аркушів 4, 5, 7 через вектор, як план.
3. **З вектора аркуша 2 зняти ще:** по 4 вентилятори аерації біля кожного силоса і розкладку каналів аерації в колі. Це видимі вузли на нашому аркуші, тобто прийнятне джерело за PRD.
4. **SLICE-7, нутро:** підлога і канали аерації, люк, камера всередині силоса.
5. **Режисер:** траєкторія камери «обліт → люк → всередину → розріз».

## 7. Як запускати

Windows (Blender 4.5.2 LTS):

```
blender --background --factory-startup --python blender/assemble_site.py -- blender/site/SITE_tech_2024_06_06.json
python blender/site/run_qa_tests.py --blender blender
```

Linux або хмара (модуль `bpy==4.5.2` з PyPI, Python 3.11; для EEVEE потрібні `libegl1 libegl-mesa0`):

```
pip install bpy==4.5.2 pymupdf
python blender/assemble_site.py blender/site/SITE_tech_2024_06_06.json
python blender/site/run_qa_tests.py
python inbox/extract_plan_p2.py
python inbox/add_sheet_marks.py
```

## 8. Інші напрями проєкту

- **2D AutoCAD** (грудень 2025): MCP-сервер `autocad-industrial` і генерація 2D-схем з `config_*.json`; мета «7-й силос копіюванням» (`README_FOR_NEXT_AGENT.md`). Для 3D не використовується. `autocad-mcp/` у репозиторій не входить.
- **Revit** (січень 2026): прототип `revit-mcp/`, запуску вживу не зафіксовано. Офіційний Revit MCP вимагає Revit 2027.
- **Бізнес-рамка** (`по3де/обота по 3-д світам.txt`): ядро — точна розрізана модель і жорсткий розбір вузла. 3D тут як доказ експертизи, а не імітація будівництва.
